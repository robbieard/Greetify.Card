// Refactored FullScreenInteractiveCard - Now using modular components
import FullScreenCard from './FullScreenCard'

const FullScreenInteractiveCard = (props) => {
  return <FullScreenCard {...props} />
}

export default FullScreenInteractiveCard